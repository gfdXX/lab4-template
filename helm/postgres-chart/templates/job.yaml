apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "postgres-chart.fullname" . }}-init
  labels:
    {{- include "postgres-chart.labels" . | nindent 4 }}
spec:
  template:
    metadata:
      labels:
        {{- include "postgres-chart.selectorLabels" . | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      containers:
      - name: db-init
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: PGPASSWORD
          value: {{ .Values.postgres.password | quote }}
        command: ["bash", "-c", "chmod +x /init-databases.sh && /init-databases.sh"]
        volumeMounts:
        - name: init-script
          mountPath: /init-databases.sh
          subPath: init-databases.sh
          readOnly: true
      volumes:
      - name: init-script
        configMap:
          name: {{ include "postgres-chart.fullname" . }}-init-script
          defaultMode: 0755

