fullnameOverride: payment-service

image:
  repository: ""  # Will be set in CI/CD
  tag: "latest"

service:
  port: 8050

env:
  - name: DATABASE_URL
    value: "postgresql://program:test@postgres:5432/payments"

command:
  - uvicorn
  - services.payment_service.main:app
  - --host
  - 0.0.0.0
  - --port
  - "8050"

